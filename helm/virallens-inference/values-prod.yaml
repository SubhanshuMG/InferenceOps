# Production environment overrides
replicaCount: 5

image:
  tag: v1.0.0

env:
  NODE_ENV: production
  LOG_LEVEL: warn

resources:
  requests:
    cpu: 500m
    memory: 512Mi
  limits:
    cpu: 2000m
    memory: 1Gi

hpa:
  minReplicas: 5
  maxReplicas: 50

topologySpreadConstraints: true
